
fn gcd x y =
  with a = x, b = y loop
    if zero? b then a
    else repeat b, a % b

fn lcm a b
   | zero? a = 0
   | zero? b = 0
   | otherwise = b * int (a / (gcd a b))

fn divisors n = reduce lcm 1 [2..n]

divisors 20 |> println!
